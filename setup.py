from setuptools import setup

if __name__ == "__main__":
    setup(
        name='XMem',
        version='0.0.1',
        description="XMem mask tracking",
        install_requires=[],
        package_dir={
            "XMem": ".",
        }
    )
